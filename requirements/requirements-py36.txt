-r requirements-base.txt

# Python 3.6 legacy stack. BLE is intentionally omitted because maintained
# bleak releases no longer support this runtime reliably.
async-timeout==3.0.1
attrs<22
chardet<4
idna-ssl==1.1.0
multidict==4.7.6
typing-extensions<4.2
yarl==1.5.1
scapy>=2.4.5,<2.5
